#include<stdio.h>
int main()
{ int a=7,b=3.14,c;
printf("the radius of circle = %d \n the value of pai = 3.14",a);
c=a*a*b;
    printf(" \n the area of circle = a*a*b=%d",c);
    printf(" \n \"Area of circle is A having the radius R\". Replace A with area & R with radius.");
    return 0;
}

